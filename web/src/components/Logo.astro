---
interface Props {
  size?: number;
  stroke?: string;
  fillDots?: string;
}
const { size = 22, stroke = "currentColor", fillDots = "currentColor" } = Astro.props;
---
<svg width={size} height={size} viewBox="0 0 26 26" fill="none" aria-hidden="true">
  <path d="M3 10 L6 3 L10 8 H16 L20 3 L23 10 V16 A10 7.5 0 0 1 3 16 Z" fill="none" stroke={stroke} stroke-width="2" stroke-linejoin="round" />
  <circle cx="9.5" cy="14" r="1.6" fill={fillDots} />
  <circle cx="16.5" cy="14" r="1.6" fill={fillDots} />
</svg>
