# 変更履歴

新しい版で何が変わったかをここに残す。更新の仕方は [README の「更新する」](README.md#更新する) を参照。

記録はバージョン 0.4.0 から始めている。

## 0.4.1

### 修正

- 署名用の証明書が無い Mac で導入すると、ビルドを待たせた末に原因の分からないエラーで失敗していた。ビルドが始まる前に、証明書の作り方 (Xcode での操作手順) を示して止まるようにした
- 導入時の他のエラー (swift が無い、中間証明書が期限切れ) にも、次に何をすればよいかの手順を添えた

## 0.4.0

### 追加

- 相手の声だけの録音を別に残すようにした。再生時に「自分と相手」と「相手だけ」を選べる。会議アプリでマイクを切っていた場面でも、混ぜた録音には自分の環境音が入るため
- 保存容量を確認して、指定した日数より古いセッションの録音だけを消せるようにした。文字起こしと要約は残る
- 履歴の一覧に、要約の先頭 1 行 (要約が無ければ最初の発言) を添えるようにした。名前を付けていないセッションが日時の羅列にならないようにするため
- 記録を止めたあと、メニューバーのパネルから直前のセッションを開けるようにした。要約ができるまでは作成中であることが出る
- 設定に「セッション」タブを追加し、名前の付け方・自動開始・無音の確認をここにまとめた

### 変更

- 音声の設定を開いただけではマイクを使わないようにした。「マイクを試す」を押した間だけ使う。Bluetooth のイヤホンでは、マイクを使うと機器が通話用に切り替わり、聞こえる音の大きさが変わるため
- 続いている異常 (マイクが許可されていない、相手の音声を取得できていない等) を「了解」で消せないようにした。小さく畳むだけにして、直るまでメニューバーの印を残す。消せると、自分の声が 1 文字も残らないまま見た目だけ正常に戻るため
- 設定から、不具合報告用の診断情報の書き出しを外した

### 修正

- セッション中に音の出力先を切り替えても、相手の声を録り続けるようにした。以前は耳には聞こえたまま、録音と文字起こしにだけ何も入らなくなり、エラーも出なかった
- 自分は話しているのに相手の声が 10 分以上届いていない場合に知らせるようにした
- 繰り返しの予定を「今後録らない」にしたとき、次の回にも効くようにした。以前は回ごとに別の予定として扱われ、押すたびに「録らない予定」が増えていた
- アプリが強制終了しても、書きかけの録音を次の起動時に復旧するようにした
- 会議の予告に出る「今後録らない」が 2 行に折り返していたのを直した
- 設定のタブが表示されないことがあったのを直した
- セッションを削除するときの確認文で、何が消えるかの説明が画面によって違っていたのを揃えた
