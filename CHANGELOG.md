# 変更履歴

新しいバージョンで何が変わったかをここに残す。更新の仕方は [README の「更新する」](README.md#更新する) を参照。

記録はバージョン 0.4.0 から始めている。

書き方:

- バージョンの見出しの下に、そのバージョンを一言でまとめる 1 行を置く。アプリの「アップデート」画面にも出る
- 1 項目は「**変わった対象** 何が起きるか」の形にする。対象は画面や機能の名前、説明は 1〜2 文
- 読み手はアプリの利用者なので、設定画面の説明文と同じ敬体で書き、文末に句点を打つ
- 修正の項目は、読み手が気づいていた症状を書く。文末は対処に合う動詞にする
- なぜ直したか、内部でどうなっていたかは書かない。操作の説明は設定画面の説明文に置く

## 0.12.0

はじめての人を、家の中まで案内するようにしました。

### 追加

- **AI の準備** ようこそ画面に、Apple Intelligence・Claude Code・Codex が使える状態かを表示するページを追加しました。「もう一度探す」で検出をやり直し、「設定を開く」で設定の AI ページへ移動できます。

### 変更

- **ようこそ画面** 初回起動の案内を、使う順番 (始め方 → 会議中にできること → 終わったあと → 使う準備 → AI の準備) の 6 ページに作り直しました。各ページに画面の絵を添え、文字だけの説明をやめました。設定の「使い方と権限を確認」からも同じ画面を開けます。
- **使う準備** 許可の一覧にシステム音声を加えました。相手の声を録音するための許可で、初めて記録を始めるときに macOS が確認します。

## 0.11.0

聞いた話を、探して引き出せるようになりました。

### 追加

- **グループへの質問** グループの画面に「質問」を追加しました。そのグループの会議をまとめて材料にして質問できます。材料が多すぎるときは古い会議から外し、外した会議の名前を残します。セッションが 1 件も無いグループでは押せません。
- **文字起こしの検索** 過去のセッションの文字起こしで Cmd+F を押すと検索欄が開き、一致した行へ順に移動できます。会議中で行が増えている間も、新しい行が検索の対象に入ります。
- **文字起こしのコピー** 行をクリック、Shift クリック、Cmd クリック、ドラッグで選び、Cmd+C か右クリックで「[時刻] 話者: 発言」の形にまとめてコピーできます。
- **質問パネルの検索** 質問と回答のパネルでも Cmd+F で検索できます。一致した件数と現在位置が出て、畳まれている中にヒットがあれば開いて見せます。
- **グループ削除の選び方** グループを削除するときに、中のセッションを未分類へ戻すか、一緒に消すかを選べるようにしました。録音中のセッションは選んでも消さず、未分類へ戻します。
- **録音の止め忘れの確認** 「お疲れ様でした」のような締めの言葉が聞こえたら、セッションを止めるかを画面の右上で確認します。自動では止めません。

### 変更

- **文字起こしの選択** 文字単位のドラッグ選択から、行単位の選択に変えました。
- **決まったことの並び** 状態の表示を議題名の左へ移し、時刻とあわせて幅を固定しました。行ごとの縦のずれがなくなります。
- **提案カードの説明文** 薄い色から、本文と同じ濃さにしました。

## 0.10.1

もう一度だけ、毛づくろいし直しました。

### 変更

- **変更履歴の並び** 「追加」「変更」「修正」を左の欄に移して縦の罫線で中身と分け、項目の間を広げました。1 件がどこで終わるのかが分かるようになります。

## 0.10.0

あちこち毛づくろいして、身なりを整えました。

### 追加

- **アップデートのページ** 設定に「アップデート」を追加し、いま使っているバージョン・更新の実行・変更履歴をここにまとめました。変更履歴は新しい順に 5 件まで出て、使用中のバージョンとまだ入っていないバージョンが分かります。
- **今すぐアップデート** 設定のボタン 1 つで更新を始められるようにしました。押すとアプリが終了し、取得とビルドは終了後もそのまま進みます。セッション中は押せません。
- **セッション名の自動命名** 名前を付けていないセッションに、要約から短い題名を付けるようにしました。日時だけの名前が一覧に並ばなくなります。手動で付けた名前はそのままです。
- **CLI でのセッション指定** `hearcat read` などで、セッション名からセッションを選べるようにしました。`help` も追加しました。

### 変更

- **設定の並び** 上のタブから左の一覧に変えました。項目が増えても畳まれずに並びます。
- **開始ボタン** パネルの開始操作を、大きなボタン 1 つとモードの切り替えにまとめました。前回選んだ録音と文字起こしの組み合わせを覚えます。保存先と資料フォルダは 1 行にまとめ、終了はメニューの奥に移しました。
- **履歴のサイドバー** フォルダは見出し 1 行だけにし、中身は右のグループの画面で見るようにしました。未分類は今日・昨日・今週・今月・それ以前に分け、今月とそれ以前は畳んだ状態で開きます。
- **決まったことの抽出** 要約と同時ではなく、要約のあとに分けて行うようにしました。会議で方針が変わったことを取りこぼしにくくなります。

### 修正

- **セッションの改名** 改名すると決まったことの記録が旧い名前と新しい名前で重複し、詳細画面からその会議へ移動できなくなっていたのを修正しました。

## 0.9.1

暗がりに紛れた文字を、見つけました。

### 修正

- **メニューバーのパネル** 保存先の選択肢が、暗い背景に黒い文字で表示されて読めなくなっていたのを修正しました。

## 0.9.0

日なたでも過ごせるようになりました。

### 追加

- **ライトモード** Mac の外観設定に合わせて、パネルとライブ画面が明るい配色に切り替わるようにしました。相手に送る共有画像は、受け取る側の環境で変わらないよう従来の見た目のままにしました。
- **指の形のカーソル** ボタンなど押せる要素にマウスを乗せると、指の形のカーソルが出るようにしました。

### 変更

- **入力感度** スライダーをレベルメーターと一体にし、マイクの音量を見ながらしきい値を決められるようにしました。

### 修正

- **セッション詳細** 「共有」ボタンの文字が、隣のボタンより下にずれていたのを修正しました。
- **質問パネル** 開いた直後に、閉じるボタンにアウトラインが表示されないように修正しました。

## 0.8.0

余計な音は、聞かなかったことにします。

### 追加

- **決定カード** カードをタップすると、その議題の変遷をその場で開けるようにしました。

### 修正

- **録音の音質** 録音に乗っていたノイズを取り除きました。
- **長い会議の要約** 要約と決定事項の抽出が途中で打ち切られていたのを、上限を 10 分に延ばしました。打ち切られたときも、途中までの結果を残すようにしました。

## 0.7.0

決まるまでの道のりに、猫ちゃんが足跡を残します。

### 追加

- **会話の外の記録** Slack や PR レビューなど、会議の外で決まったことを手で書き足せるようにしました。
- **まだ決まっていないこと** 保留のまま持ち越している議題が残っているときだけ、メニューバーのパネルに最新 3 件を出すようにしました。
- **回答するモデルの表示** 質問パネルの下に、回答するエンジンとモデル名を常に出すようにしました。

### 変更

- **決定の変遷** タイムラインを肉球の足あとが続くデザインにし、最新の決定に「いまここ」を付けました。
- **「決まったこと」タブ** 一覧を「前回の会話で決まった・変わったこと」と「そのほかの議題」に分け、同じ議題が両方に出ないようにしました。
- **決定まわりの文言** 「会議」を「会話」に揃えました。
- **質問パネル** 開くときにメニューバーのパネルを先に閉じ、手前に残らないようにしました。発話がまだ無いセッションで開いたときも、失敗表示ではなく入力待ちで開くようにしました。
- **質問の回答** エンジンを切り替えても、前の回答の表示が変わらないようにしました。

## 0.6.0

決まったことを、忘れないようにくわえておきます。

### 追加

- **決まったことの記録** 会議で決まったことを、いつ・どの会議で・何が・なぜ変わったかまで含めて、議題ごとにグループ単位で残すようにしました。
- **グループの画面** 履歴でグループ名を選ぶと開く画面を追加し、決まったことの検索・絞り込み・変遷の表示・アーカイブができるようにしました。
- **過去の会議の取り込み** まだ取り込んでいない会議から、決まったことを後から取り込めるようにしました。
- **セッション詳細の決定ブロック** その会議で決まったことを、修正・該当時刻への移動・記録から除外できるようにしました。
- **決定の経緯の回答** 質問パネルで経緯を聞くと、AI の文章ではなく記録から描いたタイムラインが出るようにしました。
- **決まったことを踏まえた回答** 質問パネルの AI が、そのグループの決定一覧を常に踏まえて答えるようにしました。

### 変更

- **質問パネルの時刻表示** 色をアプリ全体の配色に合わせました。

## 0.5.1

Codex も、話の続きを覚えるようになりました。

### 追加

- **Codex での質問** 回答が少しずつ表示され、前の回答を覚えたまま追加質問できるようにしました。
- **Codex の既定モデル** 質問や自動要約に Codex を選ぶと、設定のモデル欄に Codex 側の既定モデルが自動で入るようにしました。

## 0.5.0

気になったことを、その場で聞けるようになりました。

### 追加

- **質問パネル** 「関連資料との照合」を、会話の内容を AI に質問できるパネルに作り替えました。入力欄から質問を送り、空のまま送ると直近の会話の調査になります。
- **過去のセッションへの質問** 会議中だけでなく、終わったセッションにも質問できるようにしました。履歴の一覧と詳細画面から開けます。
- **回答の形** 判定の 1 文から始まる短い文章にし、ファイル名などの詳しい根拠は「根拠と補足」を開いたときだけ出すようにしました。確認や作業が要るときは「次の一手」が付きます。
- **聞き返し** 質問が曖昧なときは、AI が選択肢で聞き返し、クリックで答えられるようにしました。
- **根拠の時刻** 回答が根拠に挙げた時刻をクリックすると、文字起こしの該当箇所へ移動できるようにしました。
- **会話の積み重ね** 同じ対象への質問と回答が、会話として積み重なるようにしました。Claude では前の回答を覚えたまま追加質問でき、回答が届くまでの途中経過も出ます。
- **資料フォルダなしの回答** 資料フォルダを紐付けていなくても、文字起こしだけを根拠に回答するようにしました。結果の画面からその場で紐付けられます。

### 変更

- **時刻の表示** 録音開始からの経過時間 (分:秒) に統一しました。
- **質問に答える AI の権限** ファイルの読み取り以外 (コマンドの実行など) をできないよう、制限を締め直しました。
- **導入時の証明書** 署名証明書が複数入っている Mac では、使えるものを自動で選ぶようにしました。

### 修正

- **追加質問の失敗** 失敗すると、それまでの質問と回答が画面から消えていたのを修正しました。会話を残したまま、失敗した質問の下のエラー表示から再試行できます。
- **調査のキャンセル** キャンセルしたときに、アプリごと終了してしまうことがあったのを修正しました。
- **見出しの無い回答** 本文が表示されないことがあったのを修正しました。
- **日本語入力** 変換中に Enter を押すと、変換の確定ではなく質問が送信されていたのを修正しました。
- **コピーボタン** 押したあと、完了表示が出ている間だけ周囲のボタンがずれていたのを修正しました。

## 0.4.2

要約は、頼まれたとおりの形で返します。

### 修正

- **Claude での要約** Claude Code に入れている個人設定 (口調・書式の指示や hook) に影響され、要約ではない文章や崩れた書式で返ることがあったのを修正しました。個人設定を読み込まずに実行します。
- **形式が違う応答** 指示した形式でない応答を、そのまま要約として表示していたのを修正しました。何が返ってきたかを添えたエラーで知らせます。

## 0.4.1

入り口でつまずかないようにしました。

### 修正

- **証明書が無い Mac への導入** ビルドを待たせた末に、原因の分からないエラーで失敗していたのを修正しました。ビルドが始まる前に、証明書の作り方 (Xcode での操作手順) を示して止まります。
- **導入時のそのほかのエラー** swift が無い、中間証明書が期限切れといった場合にも、次に何をすればよいかの手順を添えるようにしました。

## 0.4.0

相手の声だけ、別に取っておきます。

### 追加

- **相手だけの録音** 相手の声だけの録音を別に残すようにしました。再生時に「自分と相手」と「相手だけ」を選べます。
- **古い録音の削除** 保存容量を確認して、指定した日数より古いセッションの録音だけを消せるようにしました。文字起こしと要約は残ります。
- **履歴の 1 行紹介** 一覧に、要約の先頭 1 行 (要約が無ければ最初の発言) を添えるようにしました。
- **直前のセッションを開く** 記録を止めたあと、メニューバーのパネルから直前のセッションを開けるようにしました。要約ができるまでは、作成中であることが出ます。
- **設定の「セッション」タブ** 名前の付け方・自動開始・無音の確認を、ここにまとめました。

### 変更

- **音声の設定** 開いただけではマイクを使わないようにし、「マイクを試す」を押した間だけ使うようにしました。
- **続いている異常の表示** 「了解」で消せないようにし、小さく畳むだけにしました。直るまで、メニューバーに印が残ります。
- **診断情報の書き出し** 設定から外しました。

### 修正

- **出力先の切り替え** セッション中に音の出力先を切り替えると、録音と文字起こしに相手の声が入らなくなっていたのを修正しました。
- **相手の声が届かないとき** 自分は話しているのに相手の声が 10 分以上届いていない場合に、知らせるようにしました。
- **繰り返しの予定** 「今後録らない」が次の回に効かず、押すたびに「録らない予定」が増えていたのを修正しました。
- **強制終了後の録音** アプリが強制終了しても、書きかけの録音を次の起動時に復旧するようにしました。
- **会議の予告** 「今後録らない」が 2 行に折り返していたのを修正しました。
- **設定のタブ** 表示されないことがあったのを修正しました。
- **セッション削除の確認文** 何が消えるかの説明が画面によって違っていたのを揃えました。
