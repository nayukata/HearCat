# 変更履歴

新しい版で何が変わったかをここに残す。更新の仕方は [README の「更新する」](README.md#更新する) を参照。

記録はバージョン 0.4.0 から始めている。

## 0.9.1

### 修正

- メニューバーのパネルで、壁紙の明暗によってはグループの選択肢が「暗い地に黒文字」になって読めなくなっていたのを直した。パネルの外観がメニューバー由来の外観とシステム設定の間で割れていたため、システム設定に揃えて一枚にした

## 0.9.0

### 追加

- ライトモードに対応した。これまで常にダークだったメニューバーのパネル・質問パネル・ライブ画面・お知らせも、OS の外観設定に合わせて明るい配色 (霧の薄灰と針葉樹の深緑) に切り替わる。ダークモードの見た目は従来のまま。相手に送る共有画像は、受け取る側の環境に左右されないよう外観設定に関わらず従来の見た目で出力する
- ボタンなど押せる要素にマウスを乗せると、指カーソルが出るようにした

### 変更

- 入力感度のスライダーをレベルメーターと一体化した。マイクの音量が溝に流れ、つまみで「ここ未満は文字起こしに流さない」しきい値を音量を見ながら調整できる。「マイクを試す」ボタンは廃止し、感度を手動にしている間はレベル表示のためにマイクを使う

### 修正

- セッション詳細の「共有」ボタンの文字が、隣のボタンより下にずれて見えていたのを直した
- 質問パネルを開いた直後に、閉じるボタンへ枠が表示されていたのをやめた

## 0.8.0

### 追加

- 決定カードをタップすると、その議題の全履歴タイムラインをその場で展開できるようにした (グループ詳細の展開と同じ操作感)

### 修正

- 録音の再生に「ザー」というヒスノイズが乗っていたのを直した。自分の声を自動で底上げする処理がマイクのノイズも一緒に増幅していたため撤去し、聞こえている音を無加工のまま録るようにした。自分の声が小さい場合は、マイクやオーディオインターフェース側の入力ゲインを上げてから、設定の録音音量スライダーで微調整する
- 長い会議で要約と決定事項の抽出が 5 分の上限を超えて打ち切られていたのを、上限 10 分に延長した。打ち切られた場合も途中までの出力を残し、原因を後から確認できるようにした

## 0.7.0

### 追加

- Slack や PR レビューなど、会話の外で決まった・変わったことを手動で記録できるようにした。「決まったこと」タブの議題の行にあるチェックボタンから内容を書いて確定する。手動の記録は変遷のタイムラインに輪郭だけの足あととして並び、会話からの再取り込みでも消えない
- メニューパネルに「まだ決まっていないこと」カードを追加した。セッション中、保存先グループに保留・仮のまま持ち越している議題が残っているときだけ最新 3 件を出し、会議に入ってすぐ前回までの宿題を思い出せる
- 質問パネルの下部に、これから答えるエンジンとモデル名を常時表示するようにした。回答が始まると、実際に使われているモデル名の実測値へ切り替わる

### 変更

- 決定の変遷タイムラインを、肉球の足あとが破線で続くデザインに変えた。新しい順に並び、最新の決定に「いまここ」が付く
- 「決まったこと」タブの一覧を「前回の会話で決まった・変わったこと」と「そのほかの議題」に分け、同じ議題が両方に重複して出ないようにした
- 決定まわりの文言を「会議」から「会話」に揃えた (会議以外の通話や雑談でも決まったことは記録されるため)
- 質問パネルを開いたとき、メニューパネルが手前に残って隠れてしまうことがあったのを、先にメニューパネルを閉じるようにした
- 録音中の会話にまだ発話が無い状態で質問パネルを開くと「文字起こしが無い」と失敗表示になっていたのを、入力待ちのまま開くようにした
- 質問のエンジンを切り替えると、切り替え前の回答の表示まで変わることがあったのを、その回答を実際に処理したエンジンの表示で固定した

## 0.6.0

### 追加

- 会議で決まったことを、グループ単位で記録するようにした。停止後の自動要約と合わせて AI が抽出し、議題ごとに「いつ・どの会議で・何が・なぜ」変わったかを積み重ねていく。その場限りの段取りや作業依頼は記録しない
- 履歴でグループ名を選ぶと開く、グループ専用の画面を追加した。「セッション」と「決まったこと」のタブがあり、決まったことは検索・状態 (確定/仮/保留) での絞り込み・変遷のタイムライン表示・アーカイブができる
- まだ取り込んでいない過去の会議から、決まったことを後から取り込むボタンを追加した。取り込み済みの会議はスキップし、進捗表示と中断もできる
- セッション詳細に「この会議で決まった・変わったこと」のブロックを追加した。内容の修正・該当時刻への移動・記録から外す操作ができる
- 質問パネルで決定の経緯を聞くと (「いつ変わった？」「元々どうだった？」など)、AI の文章ではなく記録から直接描いたタイムラインカードで変遷が出るようにした。日付・理由・会議名が AI の言い換えで揺れることがなく、カードは回答本文より先に表示される。理由は琥珀色の縦線付きで決定内容の直下に出て、時刻をクリックするとその会議の文字起こしへ移動できる
- 質問パネルの AI が、そのグループで決まったことの一覧 (現在値の索引) を常に踏まえて答えるようにした。索引は数 KB に収まる形で渡し、トークンの消費を抑えている

### 変更

- 質問パネルの時刻表示の色が青のままだった箇所を、アプリ全体の配色 (琥珀) に統一した

## 0.5.1

### 追加

- 質問パネルで Codex を選んだときも、回答が少しずつ表示され、前の回答を覚えたまま追加質問できるようにした。これまで Codex は毎回新規の質問扱いで、回答も完成するまで表示されなかった
- 設定で質問や自動要約に Codex を選ぶと、モデル欄に Codex 側の既定モデルが自動で入るようにした。どのモデルで動くのか分からない空欄のままにならない

## 0.5.0

### 追加

- 「関連資料との照合」を、会話の内容を AI に質問できるチャット型のパネルへ作り替えた。質問は入力欄から送り、空のまま送信すると直近の会話の調査になる
- 会議中だけでなく、過去のセッションにも質問できるようにした。履歴の一覧と詳細画面から開ける
- 回答は判定の 1 文から始まる短い文章にし、ファイル名などの詳しい根拠は「根拠と補足」を開いたときだけ出す。実際に確認・作業すべきことがあるときは「次の一手」が付く
- 質問が曖昧なときは AI が選択肢で聞き返し、クリックで答えられるようにした
- 回答が根拠として挙げた時刻は、クリックで文字起こしの該当箇所へ移動できる
- 同じ対象への質問と回答は会話として積み重なり、Claude では前の回答を覚えたまま追加質問できる。回答が届くまでの途中経過も表示される
- 資料フォルダを紐付けていなくても文字起こしだけを根拠に回答し、結果の画面からその場で紐付けられる

### 変更

- 時刻の表示を録音開始からの経過時間 (分:秒) に統一した
- 質問に答える AI がファイルの読み取り以外 (コマンド実行など) をできないよう、制限を締め直した
- 導入時、署名証明書が複数入っている Mac では使えるものを自動で選ぶようにした

### 修正

- 追加質問が失敗すると、それまでの質問と回答が画面から消えていた。会話を残したまま、失敗した質問の下のエラー表示から再試行できるようにした
- 調査をキャンセルしたときに、アプリごと終了してしまうことがあった
- 見出しの無い回答で、本文が表示されないことがあった
- 日本語入力の変換中に Enter を押すと、変換の確定ではなく質問が送信されてしまっていた
- コピーボタンを押すと、完了表示が出ている間だけ周囲のボタンがずれていた

## 0.4.2

### 修正

- Claude での要約が、利用者自身が Claude Code に入れている個人設定 (口調・書式の指示や hook) に影響され、要約ではない文章や崩れた書式で返ることがあった。個人設定を読み込まずに実行するようにした
- AI の応答が指示した要約の形式でなかったとき、その文章をそのまま要約として表示していた。何が返ってきたかを添えたエラーで知らせるようにした

## 0.4.1

### 修正

- 署名用の証明書が無い Mac で導入すると、ビルドを待たせた末に原因の分からないエラーで失敗していた。ビルドが始まる前に、証明書の作り方 (Xcode での操作手順) を示して止まるようにした
- 導入時の他のエラー (swift が無い、中間証明書が期限切れ) にも、次に何をすればよいかの手順を添えた

## 0.4.0

### 追加

- 相手の声だけの録音を別に残すようにした。再生時に「自分と相手」と「相手だけ」を選べる。会議アプリでマイクを切っていた場面でも、混ぜた録音には自分の環境音が入るため
- 保存容量を確認して、指定した日数より古いセッションの録音だけを消せるようにした。文字起こしと要約は残る
- 履歴の一覧に、要約の先頭 1 行 (要約が無ければ最初の発言) を添えるようにした。名前を付けていないセッションが日時の羅列にならないようにするため
- 記録を止めたあと、メニューバーのパネルから直前のセッションを開けるようにした。要約ができるまでは作成中であることが出る
- 設定に「セッション」タブを追加し、名前の付け方・自動開始・無音の確認をここにまとめた

### 変更

- 音声の設定を開いただけではマイクを使わないようにした。「マイクを試す」を押した間だけ使う。Bluetooth のイヤホンでは、マイクを使うと機器が通話用に切り替わり、聞こえる音の大きさが変わるため
- 続いている異常 (マイクが許可されていない、相手の音声を取得できていない等) を「了解」で消せないようにした。小さく畳むだけにして、直るまでメニューバーの印を残す。消せると、自分の声が 1 文字も残らないまま見た目だけ正常に戻るため
- 設定から、不具合報告用の診断情報の書き出しを外した

### 修正

- セッション中に音の出力先を切り替えても、相手の声を録り続けるようにした。以前は耳には聞こえたまま、録音と文字起こしにだけ何も入らなくなり、エラーも出なかった
- 自分は話しているのに相手の声が 10 分以上届いていない場合に知らせるようにした
- 繰り返しの予定を「今後録らない」にしたとき、次の回にも効くようにした。以前は回ごとに別の予定として扱われ、押すたびに「録らない予定」が増えていた
- アプリが強制終了しても、書きかけの録音を次の起動時に復旧するようにした
- 会議の予告に出る「今後録らない」が 2 行に折り返していたのを直した
- 設定のタブが表示されないことがあったのを直した
- セッションを削除するときの確認文で、何が消えるかの説明が画面によって違っていたのを揃えた
